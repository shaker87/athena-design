import React from 'react';
import Navbar from '../../SharedComponents/Navbar/Navbar';
import './Header.css';

const Header = () => {
    return (
        <div>
            <Navbar></Navbar>
        </div>
    );
};

export default Header;